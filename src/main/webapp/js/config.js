/**
 * API connection settings
 *
 * WAMP users: Start Java backend with "mvn jetty:run" (port 8080),
 * then open HTML from WAMP: http://localhost/dentalclinic/login.html
 * Uncomment the line below for WAMP:
 */
// const API_BASE = 'http://localhost:8080/api';

/** Use this when opening HTML from the Java server (http://localhost:8080/login.html). */
const API_BASE = '/api';
